<%= form_for @comment, url: '#' do |f| %>
  <% if @comment.errors.any? %>
    <div id="error_explanation">
      <p class="alert alert-danger"><%= pluralize(@comment.errors.count, "error") %> prohibited this comment(s) from being saved:</h2>

      <ul>
        <% @comment.errors.full_messages.each do |message| %>
          <li><small class="text-danger"><%= message %></small></li>
        <% end %>
      </ul>
    </div>
  <% end %>
  <div class="form-group">
    <%= f.label :content %>
    <%= f.text_area :content, class: 'form-control' %>
  </div>
  <%= f.submit 'Post Comment', class: 'btn btn-primary mb-2' %>
<% end %>
